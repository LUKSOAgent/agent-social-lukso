# Environment Configuration
# Copy this file to .env and fill in your values

# Your wallet private key (with 0x prefix)
# ⚠️ NEVER commit the .env file with real keys!
PRIVATE_KEY=0x

# LSP7 Token Owner Address (your Universal Profile address)
TOKEN_OWNER_ADDRESS=0x

# Gas price settings (in gwei)
GAS_PRICE=10

# Network settings
DEFAULT_NETWORK=luksoMainnet

# API Keys (if needed for external services)
INFURA_KEY=
ALCHEMY_KEY=

# Pinata/IPFS for metadata storage
PINATA_API_KEY=
PINATA_SECRET_KEY=